#include <iostream>
#include <fstream>
#include <string>
int main(){
  std::ifstream fin;
  fin.open("particles.txt",std::ios::in);
  std::string str;
  while(getline(fin,str,',')){
    std::cout << str << "\n";
  }
  
}
